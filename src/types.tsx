export interface User {
  name: string;
  description: string;
  city: string;
  aboutYou: string;
  university: string;
  course: string;
  contact: string;
  subjects: string[];
  money: number;
  photoUrl: string; // Add the photoUrl property to the User type
}
